/**
 * Simple `next.config.js` that mirrors the existing TypeScript config.
 * Add any runtime options (for example `turbopack.root`) here if needed.
 */

/** @type {import('next').NextConfig} */
const nextConfig = {
  // keep default options; add turbopack.root if workspace root warnings appear
};

module.exports = nextConfig;
